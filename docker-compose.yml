version: '3.8'
services:
  api:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
    volumes:
      - .:/app
    networks:
      - gestion_de_commandes

networks:
  gestion_de_commandes:
    driver: bridge